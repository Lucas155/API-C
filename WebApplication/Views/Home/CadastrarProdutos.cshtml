@model WebApplication.Models.Produtos
@{

    ViewData["Title"] = "Cadastro de produtos";
}

@using (Html.BeginForm("insertProduto", "Home", FormMethod.Post, new { }))

{
    <h1>Cadastro de Produtos</h1>

    <div class="form-group">
        @Html.Label("Produto:")
        @Html.TextBoxFor(x => x.Produto, "", new { @class = "form-control", id = "Produtos", autocompleto = "off", autofocus = "true" })
    </div>
    <div class="form-group">
        @Html.Label("Preço:")
        @Html.TextBoxFor(x => x.Preco, "", new { @class = "form-control", id = "Preco", autocompleto = "off", autofocus = "true" })
    </div>
    <div class="form-group">
        @Html.Label("Sku:")
        @Html.TextBoxFor(x => x.Sku, "", new { @class = "form-control", id = "Sku", autocompleto = "off", autofocus = "true" })
    </div>

    <div class="form-group">
        <input type="submit" class="btn btn-primary" id="Button" value="Cadastrar" />
    </div>
}